@model IEnumerable<SIGE.Models.AsistenciaEstudiante>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AsistenciaFk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstudianteFk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Asistio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AsistenciaFk.CodAsistencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstudianteFk.Apellido1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asistio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodAsistenciaEstudiante">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodAsistenciaEstudiante">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodAsistenciaEstudiante">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
